name: D

on:
  push:
    branches-ignore:
      - travis-*
      - circleci-*
      - appveyor-*
      - dustmite-*
      - gh-pages

  pull_request:
    branches-ignore:
      - travis-*
      - circleci-*
      - appveyor-*
      - dustmite-*
      - gh-pages

jobs:
  build:
    name: 'Everything'
    strategy:
      matrix:
        os: [ ubuntu-latest, macOS-latest, windows-latest ]
        dc: [ dmd-latest, dmd-2.085.0, ldc-latest, ldc-1.14.0 ]
        exclude:
          - { os: macOS-latest, dc: dmd-2.085.0 }
          - { os: macOS-latest, dc: ldc-1.14.0 }

    runs-on: ${{ matrix.os }}
    continue-on-error: true
    env:
      ACTIONS_ALLOW_UNSECURE_COMMANDS: true
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
    - uses: actions/checkout@v2

    - name: 'Install cURL on Linux'
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt update
        sudo apt install libcurl4-openssl-dev

    - name: 'Install D compiler'
      uses: dlang-community/setup-dlang@v1
      with:
        compiler: ${{ matrix.dc }}

    - name: 'Run tests'
      run: dub test

    - name: 'Build: dev (debug)'
      run: dub build -c dev -b debug

    - name: 'Build: dev (plain)'
      run: dub build -c dev -b plain

    - name: 'Build: dev (release)'
      run: dub build -c dev -b release

    - name: 'Build: application (debug)'
      run: dub build -b debug

    - name: 'Build: application (plain)'
      run: dub build -b plain

    - name: 'Build: application (release)'
      run: dub build -b release

    - name: 'Generate documenation'
      if: matrix.dc == 'dmd-latest' && matrix.os == 'ubuntu-latest' && success()
      run: |
        git clone https://github.com/zorael/dialect.git
        git clone https://github.com/zorael/lu.git
        dub fetch adrdox
        dub run adrdox -- -ip --header-title="kameloso IRC bot" source dialect/source lu/source
        cp -rv .circleci generated-docs

    - name: 'Deploy documentation to GitHub Pages'
      if: matrix.dc == 'dmd-latest' && matrix.os == 'ubuntu-latest' && success()
      uses: crazy-max/ghaction-github-pages@v2.2.0
      with:
        target_branch: gh-pages
        keep_history: false
        allow_empty_commit: true
        build_dir: generated-docs
