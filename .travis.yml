language: d

sudo: required
dist: trusty

d:
  - dmd
  - ldc

os:
  - linux
  - osx

addons:
  apt:
    packages:
      - libevent-dev

script:
  - dub build -b debug --compiler=${DC}
  - dub build -b plain --compiler=${DC}